'use client';

import React from 'react';

/**
 * FloatingActions component.
 * Currently disabled per user request to hide all floating icons and WhatsApp buttons in the bottom right.
 */
export const FloatingActions: React.FC = () => {
  return null;
};


